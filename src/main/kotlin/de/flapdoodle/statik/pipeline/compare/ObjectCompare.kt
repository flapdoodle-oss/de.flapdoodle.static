package de.flapdoodle.statik.pipeline.compare

import java.util.*
import kotlin.Comparator

object ObjectCompare : Comparator<Any>{
    override fun compare(a: Any?, b: Any?): Int {
        if (a!=null && b!=null) {
            
        }
        return 0
    }
}